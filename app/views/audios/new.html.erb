
<%= simple_form_for(@audio) do |f| %>
  <%= f.input :text_url %>
  <%= f.submit "add url", class: "btn btn-primary" %>
<% end %>

<%= simple_form_for(@audio) do |f| %>
  <%= f.input :title %>
  <%= f.input :text_to_transcript, as: :text, :input_html => { :cols => 1, :rows => 5 } %>
  <%= f.submit "Transcrire", class: "btn btn-primary" %>
<% end %>
